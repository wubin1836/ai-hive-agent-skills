# Security

Never commit an AI Hive API key. Use the `AI_HIVE_API_KEY` environment variable, the local `~/.ai-hive/config.json` file with mode `0600`, or the `--api-key` option.

Report security issues privately through the repository owner's GitHub security advisory page. Do not open a public issue for credentials or undisclosed vulnerabilities.
