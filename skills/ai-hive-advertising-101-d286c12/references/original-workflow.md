# 电商产品视觉全案生成：原创实施卡

## 任务定义

- 类别：商业广告
- 适用人群：品牌市场、电商商家、广告公司、门店和内容营销团队
- 输入形态：图片输入、视频输入、商品输入
- 核心结果：从真实卖点与受众洞察出发，生成可投放、可复盘且不过度承诺的广告素材
- 交付物：卖点证据表、创意方向、广告脚本、产品关键帧、主成片、多比例版本和合规检查
- 验收指标：卖点可见度、品牌一致性、前三秒信息效率、素材可投放率

## 阶段表

| 序号 | 阶段 | 本场景动作 |
|---:|---|---|
| 1 | 定义“电商产品视觉全案生成”的目标受众、渠道、时长、比例、预算与唯一核心结果。 | 定义“电商产品视觉全案生成”的目标受众、渠道、时长、比例、预算与唯一核心结果。 |
| 2 | 建立素材与权利清单 | 建立素材与权利清单：区分自有、已授权、仅作分析和禁止公开使用的内容。 |
| 3 | 建立商品/品牌事实卡 | 建立商品/品牌事实卡：卖点、规格、价格、使用场景和禁用表述必须有依据。 |
| 4 | 脚本规划 | 脚本规划：把目标压缩为起因、转折、结果和每个镜头必须传达的信息。 |
| 5 | 分镜设计 | 分镜设计：为每个镜头写景别、机位、动作、时长、转场和可剪辑接口。 |
| 6 | 首帧/关键帧 | 首帧/关键帧：先生成并锁定人物、商品、场景、色彩和镜头方向的关键帧。 |
| 7 | 视频生成 | 视频生成：按镜头逐段生成，记录模型、参数、素材哈希、taskId 与价格快照。 |
| 8 | 质检 | 质检：逐镜检查事实、版权、人物/商品一致性、文字错误、闪烁和可剪辑性。 |
| 9 | 调用 ai_hive_list_models 查询执行当天可用模型、参数与价格，按质量、速度或成本路由。 | 调用 ai_hive_list_models 查询执行当天可用模型、参数与价格，按质量、速度或成本路由。 |
| 10 | 先用 ai_hive_generate_ecommerce_image 做一个最小小样；用户确认后才运行后续生成。 | 先用 ai_hive_generate_ecommerce_image 做一个最小小样；用户确认后才运行后续生成。 |
| 11 | 生成后使用 ai_hive_get_task 查询原任务，保留 taskId，禁止因本地超时直接重复提交。 | 生成后使用 ai_hive_get_task 查询原任务，保留 taskId，禁止因本地超时直接重复提交。 |
| 12 | 按验收指标做逐项检查；付费、批量、发送和公开发布都必须再次确认。 | 按验收指标做逐项检查；付费、批量、发送和公开发布都必须再次确认。 |

## 模型与成本决策

1. 先调用 `ai_hive_list_models`，保存查询时间和完整价格快照。
2. 检查候选工具 schema，确认输入类型、参考素材、比例、时长和模型参数真实存在。
3. 只做一个低风险小样；记录预计调用数、最坏成本和允许失败次数。
4. 用户确认后再生成；本地超时先用 `ai_hive_get_task` 查询原 `taskId`。
5. 失败时按“查原任务 → 判断可恢复性 → 经确认后重试”的顺序处理。

## 本场景重点

参考作品只用于分析结构、节奏、构图或功能目标，不逐帧复制；输出必须具有可说明的原创差异，人物、品牌、商品、音乐、字体和素材均需获得必要授权。

最终结果不应只是一段提示词，还应包含计划、素材权利、模型与参数、任务记录、可用成品及验收报告。
