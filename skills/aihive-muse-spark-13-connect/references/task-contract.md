# Meta Muse Spark 1.3接入助手：执行契约

## 输入与缺项

目标宿主、模型需求、脱敏现有配置、预算。

先列已有材料、必须补充项、可由工具读取的公开项。不完整时仅执行资料足够的部分。

## 可直接发给助手的请求

> 请根据我提供的材料完成Meta Muse Spark 1.3接入助手。先核对目标宿主，然后交付 model-availability.csv、config-diff.md、smoke-test.json。执行前告诉我哪些工具和模型实际可用，外发哪些资料以及费用上限；只做已确认的范围。

## 本任务决策规则

- 阶段1：区分Meta账号、模型API和AI-HIVE服务入口，凭据不能混用。
- 阶段2：读取当前工具目录，核对精确版本、模态与参数而不猜ID。
- 阶段3：获授权后单次小样验证，生成脱敏配置差异和回退步骤。

## 验收重点

目录没有该型号就报告不可用，不能把别的模型冒充Muse；不写入全局配置。

## 反例测试

- 用户没有提供必要材料：列缺项并交付可完成的离线部分，不编造输入。
- 指定模型不存在或没有所需模态：停在能力差异说明，不冒用其他型号。
- 工具返回task_id但无产物：保持待完成状态，按服务端建议有限查询，不重复提交付费任务。
- 用户只要求文件：不把文件自动发布、投放、发送或写回业务系统。
