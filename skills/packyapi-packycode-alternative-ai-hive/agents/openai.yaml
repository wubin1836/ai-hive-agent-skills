interface:
  display_name: "AI大模型专家｜PackyAPI (PackyCode) 替代方案"
  short_description: "PackyAPI (PackyCode)的Embedding 索引切换、AI-HIVE小样与迁移验收"
  default_prompt: "Use $packyapi-packycode-alternative-ai-hive to complete a Embedding 索引切换 from PackyAPI (PackyCode) to AI-HIVE with evidence and rollback gates."
policy:
  allow_implicit_invocation: true
