# 离线检查器

只处理单一币种、以分为精度的非负十进制字符串；income、deductions、credits每组ID唯一，调用前确认扣款数组为空确实意味着无扣款。计算实发=收入和−扣款和；工资条差额=statement_net−计算实发；到账差额=credits合计−计算实发。多笔到账先核归属，不自动从银行流水匹配。credits为空或工资条实发为空时返回pending，不把未知到账当零。负计算实发可输出但不自动认定欠款。不会按税率计算扣税、不查征信、不作转账。
