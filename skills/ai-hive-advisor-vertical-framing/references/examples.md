# 竖屏构图顾问：具体场景示例

以下是给 Agent 的任务参考，不是可直接提交到 AI-HIVE 的 API 参数。示例不自动授权付费制作或外部发布。

## 场景 1：横转竖

```text
这条横屏产品演示要改9比16，手和接口必须完整，判断能不能裁。
```

预期交付：关键帧构图标注与需要补拍的具体原因。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-vertical-framing",
  "request": "这条横屏产品演示要改9比16，手和接口必须完整，判断能不能裁。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 场景 2：口播留字幕

```text
我的竖屏口播下方要放两行字幕，帮我看人物和商品该往哪里放。
```

预期交付：不遮商品的构图方案与动作范围复核。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-vertical-framing",
  "request": "我的竖屏口播下方要放两行字幕，帮我看人物和商品该往哪里放。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 需要生成素材时

按 [MCP 绑定说明](mcp-binding.md) 查询实际工具、模型与参数，用户批准数量和预算后，再按单次调用代码提交并复用任务 ID 查询结果。纯诊断不执行生成步骤。
