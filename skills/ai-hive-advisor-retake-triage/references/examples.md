# 口播重录诊断顾问：具体场景示例

以下是给 Agent 的任务参考，不是可直接提交到 AI-HIVE 的 API 参数。示例不自动授权付费制作或外部发布。

## 场景 1：一句说错

```text
这条90秒口播把型号说错一次，看看要重录整条还是补一段。
```

预期交付：根据语义和画面连续性给出最小重录范围。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-retake-triage",
  "request": "这条90秒口播把型号说错一次，看看要重录整条还是补一段。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 场景 2：多次重录取舍

```text
这里有三版同一段，选能保留的一版，列必须重录的句子，不要追求毫无停顿。
```

预期交付：版本选择依据和有限重录清单。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-retake-triage",
  "request": "这里有三版同一段，选能保留的一版，列必须重录的句子，不要追求毫无停顿。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 需要生成素材时

按 [MCP 绑定说明](mcp-binding.md) 查询实际工具、模型与参数，用户批准数量和预算后，再按单次调用代码提交并复用任务 ID 查询结果。纯诊断不执行生成步骤。
