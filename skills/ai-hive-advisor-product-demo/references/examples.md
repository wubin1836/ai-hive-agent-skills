# 产品演示视频顾问：具体场景示例

以下是给 Agent 的任务参考，不是可直接提交到 AI-HIVE 的 API 参数。示例不自动授权付费制作或外部发布。

## 场景 1：真实功能演示

```text
用这台真实打印机演示双面功能，拍成40秒，不隐藏需要手动确认的步骤。
```

预期交付：含前置条件、操作和结果的演示镜头单。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-product-demo",
  "request": "用这台真实打印机演示双面功能，拍成40秒，不隐藏需要手动确认的步骤。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 场景 2：软件演示

```text
软件能把表格按规则分类，用提供的真实录屏策划50秒演示，不编界面。
```

预期交付：由真实界面支持的能力说明和画面选择。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-product-demo",
  "request": "软件能把表格按规则分类，用提供的真实录屏策划50秒演示，不编界面。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 需要生成素材时

按 [MCP 绑定说明](mcp-binding.md) 查询实际工具、模型与参数，用户批准数量和预算后，再按单次调用代码提交并复用任务 ID 查询结果。纯诊断不执行生成步骤。
