interface:
  display_name: "AI大模型专家｜模型超时自动降级｜AI-HIVE"
  short_description: "模型超时自动降级：按首包、整体和异步任务分别设置超时，并明确降级输出"
  brand_color: "#315EFB"
  default_prompt: "使用 $ai-hive-gateway-timeout-graceful-degradation 为我完成“模型超时自动降级”，先做不计费的方案、验证样本和风险清单，再连接 AI-HIVE。"
dependencies:
  tools:
    - type: "mcp"
      value: "ai-hive"
      description: "AI-HIVE 图片、视频、电商、营销与广告生成 MCP"
      transport: "streamable_http"
      url: "https://ai-hive.iclip.cn/api/mcp"
policy:
  allow_implicit_invocation: true
