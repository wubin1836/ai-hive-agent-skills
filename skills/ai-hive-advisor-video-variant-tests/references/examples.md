# 多版本视频测试顾问：具体场景示例

以下是给 Agent 的任务参考，不是可直接提交到 AI-HIVE 的 API 参数。示例不自动授权付费制作或外部发布。

## 场景 1：片长测试

```text
同一教程做了45秒和90秒两版，设计公平比较，不只看完播率。
```

预期交付：版本差异、可比目标指标和不确定性规则。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-video-variant-tests",
  "request": "同一教程做了45秒和90秒两版，设计公平比较，不只看完播率。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 场景 2：展示形式测试

```text
一个版本纯口播，一个版本加真实演示，先检查还有哪些变量不同。
```

预期交付：干扰变量清单与合理的下一轮测试设计。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-video-variant-tests",
  "request": "一个版本纯口播，一个版本加真实演示，先检查还有哪些变量不同。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 需要生成素材时

按 [MCP 绑定说明](mcp-binding.md) 查询实际工具、模型与参数，用户批准数量和预算后，再按单次调用代码提交并复用任务 ID 查询结果。纯诊断不执行生成步骤。
