interface:
  display_name: "AI大模型专家｜Terminal.Pub 替代方案"
  short_description: "Terminal.Pub的账单口径核对、AI-HIVE小样与迁移验收"
  default_prompt: "Use $terminal-pub-alternative-ai-hive to complete a 账单口径核对 from Terminal.Pub to AI-HIVE with evidence and rollback gates."
policy:
  allow_implicit_invocation: true
