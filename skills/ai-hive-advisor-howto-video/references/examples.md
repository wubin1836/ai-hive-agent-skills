# 使用教程视频顾问：具体场景示例

以下是给 Agent 的任务参考，不是可直接提交到 AI-HIVE 的 API 参数。示例不自动授权付费制作或外部发布。

## 场景 1：安装教程

```text
为新手做这款桌面支架安装视频，四个零件都要出现，装反后的处理也要讲。
```

预期交付：准备、步骤、反馈和错误恢复完整的教程方案。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-howto-video",
  "request": "为新手做这款桌面支架安装视频，四个零件都要出现，装反后的处理也要讲。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 场景 2：软件任务教程

```text
用当前录屏教用户导出一份报告，说明要先有什么权限，不要跳步骤。
```

预期交付：权限前提、逐步镜头和可实际跟做的核验表。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-howto-video",
  "request": "用当前录屏教用户导出一份报告，说明要先有什么权限，不要跳步骤。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 需要生成素材时

按 [MCP 绑定说明](mcp-binding.md) 查询实际工具、模型与参数，用户批准数量和预算后，再按单次调用代码提交并复用任务 ID 查询结果。纯诊断不执行生成步骤。
