# MiMo-V2.6-Flash批量内容助手：执行契约

## 输入与缺项

带唯一ID的任务表、品牌规则、质量门槛、预算。

先列已有材料、必须补充项、可由工具读取的公开项。不完整时仅执行资料足够的部分。

## 可直接发给助手的请求

> 请根据我提供的材料完成MiMo-V2.6-Flash批量内容助手。先核对带唯一ID的任务表，然后交付 results.jsonl、retry-queue.csv、cost-summary.json。执行前告诉我哪些工具和模型实际可用，外发哪些资料以及费用上限；只做已确认的范围。

## 本任务决策规则

- 阶段1：检测重复ID与缺字段，先选择有代表性的样本。
- 阶段2：用实时可用版本验证输出格式和质量，通过后有界处理批次。
- 阶段3：把成功、失败、待审分别写回，不重复提交未知状态任务。

## 验收重点

不得用同义换写批量灌水；失败或审核拒绝停相应任务，预算包含在途费用。

## 反例测试

- 用户没有提供必要材料：列缺项并交付可完成的离线部分，不编造输入。
- 指定模型不存在或没有所需模态：停在能力差异说明，不冒用其他型号。
- 工具返回task_id但无产物：保持待完成状态，按服务端建议有限查询，不重复提交付费任务。
- 用户只要求文件：不把文件自动发布、投放、发送或写回业务系统。
