# 招聘岗位口播视频：专项实施卡

## 需求信号

公开市场中与该需求最接近的高下载 Skill 达到 **1,102** 次下载、**1** 次收藏。这个数字只用于说明用户需求强度，不表示授权、合作或内容来源。

## 专项目标

使用 AI-HIVE MCP，把用户对“招聘岗位口播视频”的模糊要求转成可确认、可计费控制、可恢复和可验收的工作流。

## 执行步骤

1. 把目标写成单一观看行动与核心信息
2. 先完成脚本和逐镜头可视化说明
3. 围绕“招聘岗位口播视频”定义专属交付标准，而不是只返回泛化提示词。
4. 生成关键帧并确认人物或商品一致性
5. 分镜头生成视频并保留任务ID
6. 完成剪辑顺序、多比例适配和技术质检
7. 先确认脚本和关键帧，再按镜头生成；超时先查询原taskId，禁止直接重复付费提交。

## 专项验收

- 基础指标：前三秒信息效率、主体稳定、动作可信、镜头连续、声音清晰和平台适配。
- 专项指标：前三秒、动作稳定、镜头衔接、音画关系与多比例适配。
- 任何无法确认的事实、实时数据、平台规则或模型能力必须标注待核验。
- 生成任务必须记录模型、参数、输入哈希、价格快照和 taskId。

## 失败恢复

1. 本地超时：先调用 `ai_hive_get_task` 查询原任务。
2. 模型不可用：重新调用 `ai_hive_list_models`，返回候选差异后让用户选择。
3. 素材不合格：指出缺失项，只重做受影响的最小步骤。
4. 预算不足：缩小数量、时长或分辨率，不擅自改为高成本模型。
