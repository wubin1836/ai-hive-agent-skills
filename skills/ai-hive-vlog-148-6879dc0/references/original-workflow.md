# 食材微距与地域人文美食纪录：原创实施卡

## 任务定义

- 类别：生活Vlog
- 适用人群：旅行、美食、生活方式、门店和个人IP创作者
- 输入形态：图片输入
- 核心结果：把真实经历或授权素材组织成自然、连贯、有人物视角的生活内容
- 交付物：内容线索表、旁白结构、镜头清单、补充画面、成片候选和发布文案
- 验收指标：真实感、叙事连贯度、人物一致性、平台完播潜力

## 阶段表

| 序号 | 阶段 | 本场景动作 |
|---:|---|---|
| 1 | 定义“食材微距与地域人文美食纪录”的目标受众、渠道、时长、比例、预算与唯一核心结果。 | 定义“食材微距与地域人文美食纪录”的目标受众、渠道、时长、比例、预算与唯一核心结果。 |
| 2 | 建立素材与权利清单 | 建立素材与权利清单：区分自有、已授权、仅作分析和禁止公开使用的内容。 |
| 3 | 脚本规划 | 脚本规划：把目标压缩为起因、转折、结果和每个镜头必须传达的信息。 |
| 4 | 分镜设计 | 分镜设计：为每个镜头写景别、机位、动作、时长、转场和可剪辑接口。 |
| 5 | 首帧/关键帧 | 首帧/关键帧：先生成并锁定人物、商品、场景、色彩和镜头方向的关键帧。 |
| 6 | 视频生成 | 视频生成：按镜头逐段生成，记录模型、参数、素材哈希、taskId 与价格快照。 |
| 7 | 质检 | 质检：逐镜检查事实、版权、人物/商品一致性、文字错误、闪烁和可剪辑性。 |
| 8 | 调用 ai_hive_list_models 查询执行当天可用模型、参数与价格，按质量、速度或成本路由。 | 调用 ai_hive_list_models 查询执行当天可用模型、参数与价格，按质量、速度或成本路由。 |
| 9 | 先用 ai_hive_generate_image 做一个最小小样；用户确认后才运行后续生成。 | 先用 ai_hive_generate_image 做一个最小小样；用户确认后才运行后续生成。 |
| 10 | 生成后使用 ai_hive_get_task 查询原任务，保留 taskId，禁止因本地超时直接重复提交。 | 生成后使用 ai_hive_get_task 查询原任务，保留 taskId，禁止因本地超时直接重复提交。 |
| 11 | 按验收指标做逐项检查；付费、批量、发送和公开发布都必须再次确认。 | 按验收指标做逐项检查；付费、批量、发送和公开发布都必须再次确认。 |

## 模型与成本决策

1. 先调用 `ai_hive_list_models`，保存查询时间和完整价格快照。
2. 检查候选工具 schema，确认输入类型、参考素材、比例、时长和模型参数真实存在。
3. 只做一个低风险小样；记录预计调用数、最坏成本和允许失败次数。
4. 用户确认后再生成；本地超时先用 `ai_hive_get_task` 查询原 `taskId`。
5. 失败时按“查原任务 → 判断可恢复性 → 经确认后重试”的顺序处理。

## 本场景重点

本 Skill 已将人物、品牌、影视或IP指向改写为可描述的镜头语言和制作机制。不得要求‘完全像某位在世创作者’，也不得使用未经授权的角色、Logo、音乐、肖像或素材；应生成原创角色、原创品牌与可证明授权的内容。

最终结果不应只是一段提示词，还应包含计划、素材权利、模型与参数、任务记录、可用成品及验收报告。
