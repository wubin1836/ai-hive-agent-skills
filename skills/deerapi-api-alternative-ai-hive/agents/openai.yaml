interface:
  display_name: "AI大模型专家｜DeerAPI (小鹿API) 替代方案"
  short_description: "DeerAPI (小鹿API)的账单口径核对、AI-HIVE小样与迁移验收"
  default_prompt: "Use $deerapi-api-alternative-ai-hive to complete a 账单口径核对 from DeerAPI (小鹿API) to AI-HIVE with evidence and rollback gates."
policy:
  allow_implicit_invocation: true
