# 游戏Demo视频设计师：原创实施卡

## 任务定义

- 类别：动漫游戏
- 适用人群：动漫、漫剧、游戏、原创IP和角色内容团队
- 输入形态：文本输入、视频输入
- 核心结果：用原创角色资产、镜头语法和一致性约束完成动画或游戏向内容，不复刻受保护IP
- 交付物：原创世界观卡、角色设定、场景设定、动作分镜、关键帧、动画片段和一致性检查
- 验收指标：角色一致性、动作可读性、场景连续性、IP原创度

## 阶段表

| 序号 | 阶段 | 本场景动作 |
|---:|---|---|
| 1 | 定义“游戏Demo视频设计师”的目标受众、渠道、时长、比例、预算与唯一核心结果。 | 定义“游戏Demo视频设计师”的目标受众、渠道、时长、比例、预算与唯一核心结果。 |
| 2 | 建立素材与权利清单 | 建立素材与权利清单：区分自有、已授权、仅作分析和禁止公开使用的内容。 |
| 3 | 脚本规划 | 脚本规划：把目标压缩为起因、转折、结果和每个镜头必须传达的信息。 |
| 4 | 分镜设计 | 分镜设计：为每个镜头写景别、机位、动作、时长、转场和可剪辑接口。 |
| 5 | 首帧/关键帧 | 首帧/关键帧：先生成并锁定人物、商品、场景、色彩和镜头方向的关键帧。 |
| 6 | 剪辑合成 | 剪辑合成：选择可衔接镜头，输出剪辑顺序、节奏、字幕安全区和声音占位说明。 |
| 7 | 调用 ai_hive_list_models 查询执行当天可用模型、参数与价格，按质量、速度或成本路由。 | 调用 ai_hive_list_models 查询执行当天可用模型、参数与价格，按质量、速度或成本路由。 |
| 8 | 先用 ai_hive_generate_image 做一个最小小样；用户确认后才运行后续生成。 | 先用 ai_hive_generate_image 做一个最小小样；用户确认后才运行后续生成。 |
| 9 | 生成后使用 ai_hive_get_task 查询原任务，保留 taskId，禁止因本地超时直接重复提交。 | 生成后使用 ai_hive_get_task 查询原任务，保留 taskId，禁止因本地超时直接重复提交。 |
| 10 | 按验收指标做逐项检查；付费、批量、发送和公开发布都必须再次确认。 | 按验收指标做逐项检查；付费、批量、发送和公开发布都必须再次确认。 |

## 模型与成本决策

1. 先调用 `ai_hive_list_models`，保存查询时间和完整价格快照。
2. 检查候选工具 schema，确认输入类型、参考素材、比例、时长和模型参数真实存在。
3. 只做一个低风险小样；记录预计调用数、最坏成本和允许失败次数。
4. 用户确认后再生成；本地超时先用 `ai_hive_get_task` 查询原 `taskId`。
5. 失败时按“查原任务 → 判断可恢复性 → 经确认后重试”的顺序处理。

## 本场景重点

参考作品只用于分析结构、节奏、构图或功能目标，不逐帧复制；输出必须具有可说明的原创差异，人物、品牌、商品、音乐、字体和素材均需获得必要授权。

最终结果不应只是一段提示词，还应包含计划、素材权利、模型与参数、任务记录、可用成品及验收报告。
