# 画音同步顾问：具体场景示例

以下是给 Agent 的任务参考，不是可直接提交到 AI-HIVE 的 API 参数。示例不自动授权付费制作或外部发布。

## 场景 1：固定延迟

```text
这段视频从头到尾声音都慢一点，用拍手点量一下，保留原件。
```

预期交付：有依据的偏移测量和调整方案，实际执行需可用工具。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-av-sync",
  "request": "这段视频从头到尾声音都慢一点，用拍手点量一下，保留原件。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 场景 2：逐渐漂移

```text
五分钟视频开头同步，结尾差很多，帮我判断是不是不能只挪音轨。
```

预期交付：首中尾测量、问题分类与时间基准检查建议。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-av-sync",
  "request": "五分钟视频开头同步，结尾差很多，帮我判断是不是不能只挪音轨。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 需要生成素材时

按 [MCP 绑定说明](mcp-binding.md) 查询实际工具、模型与参数，用户批准数量和预算后，再按单次调用代码提交并复用任务 ID 查询结果。纯诊断不执行生成步骤。
