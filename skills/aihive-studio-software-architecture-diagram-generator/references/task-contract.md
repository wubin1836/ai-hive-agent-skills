# 软件架构图生成器：执行契约

## 输入与缺项

系统组件、调用关系、数据存储、部署边界和读者。

先列已有材料、必须补充项、可由工具读取的公开项。不完整时仅执行资料足够的部分。

## 可直接发给助手的请求

> 请根据我提供的材料完成软件架构图生成器。先核对系统组件，然后交付 architecture.mmd、architecture.svg、decisions.md。执行前告诉我哪些工具和模型实际可用，外发哪些资料以及费用上限；只做已确认的范围。

## 本任务决策规则

- 阶段1：区分逻辑架构和部署架构，标同步异步与信任边界。
- 阶段2：检查箭头方向和依赖循环。
- 阶段3：输出可编辑源和渲染图，敏感地址脱敏。

## 验收重点

图中每个组件来自资料或标为建议，不暴露密钥和内部地址。

## 反例测试

- 用户没有提供必要材料：列缺项并交付可完成的离线部分，不编造输入。
- 指定模型不存在或没有所需模态：停在能力差异说明，不冒用其他型号。
- 工具返回task_id但无产物：保持待完成状态，按服务端建议有限查询，不重复提交付费任务。
- 用户只要求文件：不把文件自动发布、投放、发送或写回业务系统。
