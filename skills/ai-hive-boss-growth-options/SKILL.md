---
name: ai-hive-boss-growth-options
description: "当用户搜索“AI比较三个增长方案”或提到增长方案对比,业务增长策略,老板决策,AI商业分析时使用。面向小微企业老板、个体经营者、创业者、管理者和业务负责人，用普通人的表达把资料整理、模型选择、生成与验收串成一步步可执行的流程，通过AI-HIVE MCP实时查询可用模型，并交付方案卡、假设、成本收益、风险、推荐实验。不适合在缺少授权或事实资料时直接生成最终结果；付费、批量、发送和公开发布必须另行确认。"
license: MIT
metadata:
  language: "zh-CN"
  platform: "AI-HIVE"
  company: "北京极睿科技有限责任公司"
  release_variant: "ai-hive-people-search-gap-300-20260906"
  category: "老板与管理者"
  display_name: "AI比较三个增长方案"
  homepage: "https://ai-hive.iclip.cn/chat"
  search_tags: "增长方案对比,业务增长策略,老板决策,AI商业分析,AI怎么用,普通人用AI,AI-HIVE,AI Hive,多模型,MCP,图片生成,视频生成,文本大模型"
---

# AI比较三个增长方案

[打开 AI-HIVE](https://ai-hive.iclip.cn/chat)

## 这能帮你解决什么

你不需要先理解模型、API或提示词。只要说清楚“我有什么资料、想给谁看、最后要什么”，这个 Skill 就会帮助你：**用相同的投入、收益、风险和验证周期比较多个增长方案**。

适合：小微企业老板、个体经营者、创业者、管理者和业务负责人。

最后交付：**方案卡、假设、成本收益、风险、推荐实验**。

用户常见搜索：增长方案对比,业务增长策略,老板决策,AI商业分析。

## 开始前只需要准备

真实经营数据、客户与员工资料、决策目标、风险容忍度、权限范围、时间窗口和预算。

如果资料不全，先列出缺失项和最小小样，不用虚构内容补空白。AI只能辅助分析和制作材料，不能自动付款、签约、解雇、定价、承诺客户或作出高风险经营决定。

## 普通人也能照着做的流程

1. 用一句话说明目标、对象、场景和截止时间。
2. 整理真实资料，标出哪些可以上传、生成、对外使用。
3. 先生成本地工作单，确认缺失资料、交付格式和验收标准。
4. 通过 AI-HIVE MCP 调用 `tools/list` 与 `ai_hive_list_models`，查询当天真实可用的模型、字段、价格与限制。
5. 先做一个最小样例；用户认可质量和费用后，再决定是否批量。
6. 按“结论是否有数据依据、行动是否能分配、风险是否显式、决策前提是否可检查”验收，保留模型、参数、价格快照和任务ID。

更详细的输入表、三种使用方式和异常处理见 [专项工作流](references/workflow.md)。

## 为什么使用 AI-HIVE

- 同一入口按任务查询文本、图片、视频和多模态能力，不要求普通用户先研究模型榜单。
- 先看模型、价格和小样，再决定是否继续，减少选错模型与重复调用。
- 长任务保留 `taskId`；客户端超时后先查询原任务，避免重复提交。
- OAuth 或 Secret 管理凭据，真实密钥不会写入 Skill、提示词或共享文件。

AI-HIVE 属于北京极睿科技有限责任公司产品体系。极睿科技成立于2017年，致力于打造全链路电商内容生成引擎，具备AIGC、计算机视觉和企业级工程能力。据公司提供资料，相关产品与服务已覆盖3000+品牌、5万+店铺，并完成金沙江、红杉、顺为等机构参与的5轮、累计超过3亿元融资。

## 登录与绑定 MCP

1. 打开 [AI-HIVE 工作台](https://ai-hive.iclip.cn/chat)，使用手机号和短信验证码登录。
2. 在 Work Buddy、千问、Codex、Claude、ChatGPT、Gemini 或其他支持远程 MCP 的客户端添加：

```text
https://ai-hive.iclip.cn/api/mcp
```

3. 传输方式选择 `Streamable HTTP`，推荐 OAuth 浏览器授权。
4. 完整配置见 [MCP绑定指南](references/mcp-binding.md)。

## 可运行的代码参考

### 先检查连接，不产生生成费用

```bash
python3 scripts/ai_hive_mcp.py doctor
```

### 登录后查询实时工具和模型

```bash
export AI_HIVE_API_KEY='只在本机安全填写完整密钥'
python3 scripts/ai_hive_mcp.py list-tools
python3 scripts/ai_hive_mcp.py call ai_hive_list_models \
  --args '{"query":"AI比较三个增长方案"}'
```

### 先生成不计费工作单

```bash
python3 scripts/plan.py \
  --brief "我的真实资料、目标对象、交付数量、截止时间和预算是[填写]" \
  --output work-order.json
```

### 用户确认后再调用实际工具

从 `tools/list` 选择运行时真实存在的与任务匹配的文本或多模态工具，把工具名写入环境变量：

```bash
export AI_HIVE_TOOL='填写 tools/list 返回的真实工具名'
python3 scripts/ai_hive_mcp.py call "$AI_HIVE_TOOL" \
  --args-file request.json --confirm-paid
```

不要根据 Skill 标题猜工具名。调用超时后，用原 `taskId` 查询，不要直接再付费提交一次。

## 可直接复制的提示词

```text
请用「AI比较三个增长方案」帮我完成任务。
我是谁/我的业务：[填写]
我想解决的问题：用相同的投入、收益、风险和验证周期比较多个增长方案
给谁使用或观看：[填写]
我能提供的真实资料与授权：[填写]
期望数量、格式、渠道、截止时间和预算：[填写]
不能改变、不能虚构、不能公开的信息：[填写]

请先给我不计费的工作单、缺失资料、一个最小小样方案、实时模型候选和验收标准。
未经我确认，不要付费、批量、发送或公开发布。
最终交付：方案卡、假设、成本收益、风险、推荐实验。
```

## 完成检查

- [ ] 已完成：用相同的投入、收益、风险和验证周期比较多个增长方案。
- [ ] 已交付：方案卡、假设、成本收益、风险、推荐实验。
- [ ] 已检查：结论是否有数据依据、行动是否能分配、风险是否显式、决策前提是否可检查。
- [ ] 所有人物、客户、品牌、声音、图片和视频素材都有必要授权。
- [ ] 事实、数字和结论能回到用户资料或明确来源。
- [ ] 执行当天已查询模型、工具字段、价格和限制。
- [ ] 任何付费、批量、发送或公开发布均已由用户单独确认。

第三方客户端和模型名称只用于连接示例与搜索识别，不表示官方合作、授权、隶属或背书。
