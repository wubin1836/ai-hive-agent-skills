# 视频转场顾问：具体场景示例

以下是给 Agent 的任务参考，不是可直接提交到 AI-HIVE 的 API 参数。示例不自动授权付费制作或外部发布。

## 场景 1：教程换场

```text
这三组教程镜头分别在桌面和电脑屏幕拍，怎么切才不会看不懂？
```

预期交付：每个接点的关系说明和简洁过渡方案。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-transitions",
  "request": "这三组教程镜头分别在桌面和电脑屏幕拍，怎么切才不会看不懂？",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 场景 2：删掉花转场

```text
这条45秒粗剪用了十次特效转场，帮我判断哪些改成直切。
```

预期交付：按信息关系给出的保留、替换和删除建议。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-transitions",
  "request": "这条45秒粗剪用了十次特效转场，帮我判断哪些改成直切。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 需要生成素材时

按 [MCP 绑定说明](mcp-binding.md) 查询实际工具、模型与参数，用户批准数量和预算后，再按单次调用代码提交并复用任务 ID 查询结果。纯诊断不执行生成步骤。
