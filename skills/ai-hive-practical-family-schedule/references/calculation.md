# 离线检查器

events是活动数组，id唯一，start/end须带UTC偏移且结束晚于开始；resources是实际占用的人或设备标识，不要只填参加的孩子。buffer_after_minutes为用户明确给出的活动后缓冲。区间为左闭右开，相邻无缓冲不冲突。脚本比较同一资源的活动交叠或缓冲交叠，不查询路程、创建日历或确认照护人接受。零冲突只代表已输入条件下未发现交叠，不等于整个家庭安排可行。
