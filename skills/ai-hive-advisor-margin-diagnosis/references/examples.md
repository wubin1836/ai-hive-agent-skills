# 毛利诊断顾问：具体场景示例

以下是给 Agent 的任务参考，不是可直接提交到 AI-HIVE 的 API 参数。示例不自动授权付费制作或外部发布。

## 场景 1：渠道收入增长毛利下降

```text
我提供两个季度的脱敏订单，含3个渠道和12个产品。请解释收入上升而毛利率下降的来源，先核对口径，再给五项排查顺序，不直接涨价。
```

预期交付：可回溯分组数据的毛利变化表与待验证原因

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-margin-diagnosis",
  "request": "我提供两个季度的脱敏订单，含3个渠道和12个产品。请解释收入上升而毛利率下降的来源，先核对口径，再给五项排查顺序，不直接涨价。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 场景 2：促销前后比较

```text
虚构案例：A产品售价由100降到90，单位直接成本60，销量由1000增至1500。请比较毛利额和毛利率，并说明哪些额外费用未被纳入，别把毛利当净利润。
```

预期交付：明确口径的前后比较及费用缺口说明

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-margin-diagnosis",
  "request": "虚构案例：A产品售价由100降到90，单位直接成本60，销量由1000增至1500。请比较毛利额和毛利率，并说明哪些额外费用未被纳入，别把毛利当净利润。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 需要生成素材时

按 [MCP 绑定说明](mcp-binding.md) 查询实际工具、模型与参数，用户批准数量和预算后，再按单次调用代码提交并复用任务 ID 查询结果。纯诊断不执行生成步骤。
