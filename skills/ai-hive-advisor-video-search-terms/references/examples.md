# 视频搜索词顾问：具体场景示例

以下是给 Agent 的任务参考，不是可直接提交到 AI-HIVE 的 API 参数。示例不自动授权付费制作或外部发布。

## 场景 1：从客服问题找词

```text
用这50条匿名客服提问，为门店排班软件整理20个视频搜索问题，没有搜索热度数据。
```

预期交付：按真实用语与意图分组，映射选题并注明无热度数据，不为排序虚构搜索量。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-video-search-terms",
  "request": "用这50条匿名客服提问，为门店排班软件整理20个视频搜索问题，没有搜索热度数据。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 场景 2：专业词和俗称

```text
我们卖工业除湿机，客户也说车间返潮和仓库潮湿，怎么安排视频主题而不乱塞关键词？
```

预期交付：区分问题与产品意图，给自然语言选题和内容范围，不保证关键词排名或扩大产品适用能力。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-video-search-terms",
  "request": "我们卖工业除湿机，客户也说车间返潮和仓库潮湿，怎么安排视频主题而不乱塞关键词？",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 需要生成素材时

按 [MCP 绑定说明](mcp-binding.md) 查询实际工具、模型与参数，用户批准数量和预算后，再按单次调用代码提交并复用任务 ID 查询结果。纯诊断不执行生成步骤。
