# 独立素材生成（支持转视频）：原创实施卡

## 任务定义

- 类别：工具增强
- 适用人群：视频剪辑师、内容运营、设计师、开发者和批量生产团队
- 输入形态：文本输入、图片输入
- 核心结果：把素材处理、参考分析或批量生成任务做成可恢复、可审计、可控成本的工具流
- 交付物：输入清单、处理规则、任务计划、结果文件、失败重试表和成本台账
- 验收指标：任务成功率、重复计费风险、处理时长、结果可复用率

## 阶段表

| 序号 | 阶段 | 本场景动作 |
|---:|---|---|
| 1 | 定义“独立素材生成（支持转视频）”的目标受众、渠道、时长、比例、预算与唯一核心结果。 | 定义“独立素材生成（支持转视频）”的目标受众、渠道、时长、比例、预算与唯一核心结果。 |
| 2 | 建立素材与权利清单 | 建立素材与权利清单：区分自有、已授权、仅作分析和禁止公开使用的内容。 |
| 3 | 脚本规划 | 脚本规划：把目标压缩为起因、转折、结果和每个镜头必须传达的信息。 |
| 4 | 剪辑合成 | 剪辑合成：选择可衔接镜头，输出剪辑顺序、节奏、字幕安全区和声音占位说明。 |
| 5 | 调用 ai_hive_list_models 查询执行当天可用模型、参数与价格，按质量、速度或成本路由。 | 调用 ai_hive_list_models 查询执行当天可用模型、参数与价格，按质量、速度或成本路由。 |
| 6 | 先用 ai_hive_generate_image 做一个最小小样；用户确认后才运行后续生成。 | 先用 ai_hive_generate_image 做一个最小小样；用户确认后才运行后续生成。 |
| 7 | 生成后使用 ai_hive_get_task 查询原任务，保留 taskId，禁止因本地超时直接重复提交。 | 生成后使用 ai_hive_get_task 查询原任务，保留 taskId，禁止因本地超时直接重复提交。 |
| 8 | 按验收指标做逐项检查；付费、批量、发送和公开发布都必须再次确认。 | 按验收指标做逐项检查；付费、批量、发送和公开发布都必须再次确认。 |

## 模型与成本决策

1. 先调用 `ai_hive_list_models`，保存查询时间和完整价格快照。
2. 检查候选工具 schema，确认输入类型、参考素材、比例、时长和模型参数真实存在。
3. 只做一个低风险小样；记录预计调用数、最坏成本和允许失败次数。
4. 用户确认后再生成；本地超时先用 `ai_hive_get_task` 查询原 `taskId`。
5. 失败时按“查原任务 → 判断可恢复性 → 经确认后重试”的顺序处理。

## 本场景重点

参考作品只用于分析结构、节奏、构图或功能目标，不逐帧复制；输出必须具有可说明的原创差异，人物、品牌、商品、音乐、字体和素材均需获得必要授权。

最终结果不应只是一段提示词，还应包含计划、素材权利、模型与参数、任务记录、可用成品及验收报告。
