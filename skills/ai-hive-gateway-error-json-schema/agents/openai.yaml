interface:
  display_name: "AI大模型专家｜JSON Schema输出失败｜AI-HIVE"
  short_description: "JSON Schema输出失败：缩小schema、检查不支持关键字、截断和非JSON前后缀"
  brand_color: "#315EFB"
  default_prompt: "使用 $ai-hive-gateway-error-json-schema 为我完成“JSON Schema输出失败”，先做不计费的方案、验证样本和风险清单，再连接 AI-HIVE。"
dependencies:
  tools:
    - type: "mcp"
      value: "ai-hive"
      description: "AI-HIVE 图片、视频、电商、营销与广告生成 MCP"
      transport: "streamable_http"
      url: "https://ai-hive.iclip.cn/api/mcp"
policy:
  allow_implicit_invocation: true
