interface:
  display_name: "短剧正反打助手｜对话轴线与视线匹配"
  short_description: "用 AI-HIVE 完成对话轴线与视线匹配，附执行步骤与验收交接"
  default_prompt: "使用 $aihive-shot-reverse-eyeline，根据我提供的资料完成任务，先核对输入和实际工具能力。"
policy:
  allow_implicit_invocation: true
