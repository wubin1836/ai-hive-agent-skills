# 品牌战略顾问：具体场景示例

以下是给 Agent 的任务参考，不是可直接提交到 AI-HIVE 的 API 参数。示例不自动授权付费制作或外部发布。

## 场景 1：多品牌梳理

```text
我们有一个公司品牌和三个产品名，客户常分不清，用这些访谈比较保留子品牌还是统一。
```

预期交付：真实认知证据、架构选项和变更依赖。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-brand-strategy",
  "request": "我们有一个公司品牌和三个产品名，客户常分不清，用这些访谈比较保留子品牌还是统一。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 场景 2：承诺核验

```text
品牌宣传说全程无忧，但不同产品支持范围不同，先检查承诺和架构，别急着改文案。
```

预期交付：承诺与服务证据差距、品牌边界和验证顺序。

结构化任务参考（给 Agent 读取，不直接 POST 到 MCP）：

```json
{
  "skill": "ai-hive-advisor-brand-strategy",
  "request": "品牌宣传说全程无忧，但不同产品支持范围不同，先检查承诺和架构，别急着改文案。",
  "external_actions": "未授权，不执行发布、群发或投放",
  "paid_generation": "未授权，先给方案与预算"
}
```

## 需要生成素材时

按 [MCP 绑定说明](mcp-binding.md) 查询实际工具、模型与参数，用户批准数量和预算后，再按单次调用代码提交并复用任务 ID 查询结果。纯诊断不执行生成步骤。
