# GPT-6 Luna批量表格处理助手：执行契约

## 输入与缺项

脱敏任务表、唯一行ID、分类或提取schema、验收样本和预算。

先列已有材料、必须补充项、可由工具读取的公开项。不完整时仅执行资料足够的部分。

## 可直接发给助手的请求

> 请根据我提供的材料完成GPT-6 Luna批量表格处理助手。先核对脱敏任务表，然后交付 processed.csv、failures.csv、quality-report.md。执行前告诉我哪些工具和模型实际可用，外发哪些资料以及费用上限；只做已确认的范围。

## 本任务决策规则

- 阶段1：校验输入行并将分类、提取等聚焦任务与复杂推理分开。
- 阶段2：核验Luna接口参数，先做小样结构校验和人工抽检。
- 阶段3：按已批准批次运行并输出行级错误与费用台账。

## 验收重点

不默默把困难行升级到贵模型，不将重试结果覆盖原始证据。

## 反例测试

- 用户没有提供必要材料：列缺项并交付可完成的离线部分，不编造输入。
- 指定模型不存在或没有所需模态：停在能力差异说明，不冒用其他型号。
- 工具返回task_id但无产物：保持待完成状态，按服务端建议有限查询，不重复提交付费任务。
- 用户只要求文件：不把文件自动发布、投放、发送或写回业务系统。
