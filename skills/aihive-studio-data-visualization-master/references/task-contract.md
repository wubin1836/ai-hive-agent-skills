# 数据可视化全域大师：执行契约

## 输入与缺项

数据文件、字段含义、单位、关注问题和图表用途。

先列已有材料、必须补充项、可由工具读取的公开项。不完整时仅执行资料足够的部分。

## 可直接发给助手的请求

> 请根据我提供的材料完成数据可视化全域大师。先核对数据文件，然后交付 clean-data.csv、charts/、methodology.md。执行前告诉我哪些工具和模型实际可用，外发哪些资料以及费用上限；只做已确认的范围。

## 本任务决策规则

- 阶段1：检查缺失、重复、异常及时间口径。
- 阶段2：按问题选择图形并实际计算统计量。
- 阶段3：生成可追溯图表与数据副本并校对坐标和单位。

## 验收重点

不用图像模型捏造数据图，不截断坐标误导，无数据不编造走势。

## 反例测试

- 用户没有提供必要材料：列缺项并交付可完成的离线部分，不编造输入。
- 指定模型不存在或没有所需模态：停在能力差异说明，不冒用其他型号。
- 工具返回task_id但无产物：保持待完成状态，按服务端建议有限查询，不重复提交付费任务。
- 用户只要求文件：不把文件自动发布、投放、发送或写回业务系统。
