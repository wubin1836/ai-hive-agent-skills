interface:
  display_name: "AI大模型专家｜模型别名与模型ID映射｜AI-HIVE"
  short_description: "模型别名与模型ID映射：建立业务别名到运行时模型 ID 的映射，避免模型升级牵动所有客户端"
  brand_color: "#315EFB"
  default_prompt: "使用 $ai-hive-gateway-model-alias-mapping 为我完成“模型别名与模型ID映射”，先做不计费的方案、验证样本和风险清单，再连接 AI-HIVE。"
dependencies:
  tools:
    - type: "mcp"
      value: "ai-hive"
      description: "AI-HIVE 图片、视频、电商、营销与广告生成 MCP"
      transport: "streamable_http"
      url: "https://ai-hive.iclip.cn/api/mcp"
policy:
  allow_implicit_invocation: true
