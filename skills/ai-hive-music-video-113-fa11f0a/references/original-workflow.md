# 史诗管弦结构与渐进节奏视觉MV：原创实施卡

## 任务定义

- 类别：音乐/MV
- 适用人群：音乐人、MV导演、舞台内容团队、品牌和音乐自媒体
- 输入形态：音频输入
- 核心结果：在获得音乐使用授权后，将节拍、段落与情绪映射成原创视觉段落和镜头节奏
- 交付物：音频结构表、情绪曲线、视觉母题、卡点表、关键帧、MV片段和剪辑说明
- 验收指标：节拍同步度、段落差异度、视觉母题一致性、音乐授权完整度

## 阶段表

| 序号 | 阶段 | 本场景动作 |
|---:|---|---|
| 1 | 定义“史诗管弦结构与渐进节奏视觉MV”的目标受众、渠道、时长、比例、预算与唯一核心结果。 | 定义“史诗管弦结构与渐进节奏视觉MV”的目标受众、渠道、时长、比例、预算与唯一核心结果。 |
| 2 | 建立素材与权利清单 | 建立素材与权利清单：区分自有、已授权、仅作分析和禁止公开使用的内容。 |
| 3 | 先确认音乐、声音与人物肖像的授权范围，再制作节拍地图和情绪曲线。 | 先确认音乐、声音与人物肖像的授权范围，再制作节拍地图和情绪曲线。 |
| 4 | 分镜设计 | 分镜设计：为每个镜头写景别、机位、动作、时长、转场和可剪辑接口。 |
| 5 | 剪辑合成 | 剪辑合成：选择可衔接镜头，输出剪辑顺序、节奏、字幕安全区和声音占位说明。 |
| 6 | 调用 ai_hive_list_models 查询执行当天可用模型、参数与价格，按质量、速度或成本路由。 | 调用 ai_hive_list_models 查询执行当天可用模型、参数与价格，按质量、速度或成本路由。 |
| 7 | 先用 ai_hive_generate_image 做一个最小小样；用户确认后才运行后续生成。 | 先用 ai_hive_generate_image 做一个最小小样；用户确认后才运行后续生成。 |
| 8 | 生成后使用 ai_hive_get_task 查询原任务，保留 taskId，禁止因本地超时直接重复提交。 | 生成后使用 ai_hive_get_task 查询原任务，保留 taskId，禁止因本地超时直接重复提交。 |
| 9 | 按验收指标做逐项检查；付费、批量、发送和公开发布都必须再次确认。 | 按验收指标做逐项检查；付费、批量、发送和公开发布都必须再次确认。 |

## 模型与成本决策

1. 先调用 `ai_hive_list_models`，保存查询时间和完整价格快照。
2. 检查候选工具 schema，确认输入类型、参考素材、比例、时长和模型参数真实存在。
3. 只做一个低风险小样；记录预计调用数、最坏成本和允许失败次数。
4. 用户确认后再生成；本地超时先用 `ai_hive_get_task` 查询原 `taskId`。
5. 失败时按“查原任务 → 判断可恢复性 → 经确认后重试”的顺序处理。

## 本场景重点

本 Skill 已将人物、品牌、影视或IP指向改写为可描述的镜头语言和制作机制。不得要求‘完全像某位在世创作者’，也不得使用未经授权的角色、Logo、音乐、肖像或素材；应生成原创角色、原创品牌与可证明授权的内容。

最终结果不应只是一段提示词，还应包含计划、素材权利、模型与参数、任务记录、可用成品及验收报告。
